## 2026-02-27 reproducibility run-through (F3)

- First failing command in published quick path is Step 2 (`python -m backend.llm_infrastructure.elasticsearch.cli create ...`) because README omits `ES_HOST=http://localhost:8002`; CLI defaults to `http://localhost:9200` (`backend/llm_infrastructure/elasticsearch/cli.py:44`), so create hits the wrong port and fails with connection refused.
- Step 2 is also not idempotent as written: reruns fail with `resource_already_exists_exception` unless `--skip-if-exists` is added (`backend/llm_infrastructure/elasticsearch/cli.py:241`).
- Step 4 output schema does not match Step 5 asset expectations when trying to feed fresh eval output: eval writes `top10_doc_ids` and metric keys like `hit@5`/`MRR` (`scripts/paper_b/run_paper_b_eval.py:379`, `scripts/paper_b/run_paper_b_eval.py:476`), while assets script reads `top_k_doc_ids` and metric keys `hit_at_5`/`mrr` plus `primary_mode_name` (`scripts/paper_b/generate_paper_b_assets.py:109`, `scripts/paper_b/generate_paper_b_assets.py:171`, `scripts/paper_b/generate_paper_b_assets.py:182`).
- Step 5 command in README only works with pre-baked task-10 evidence layout (`baseline_v1_nondet/`, `deterministic_protocol/`, `stability_aware_mq_v2/`); pointing `--evidence-root` at Step 4 output root fails with missing evidence (`scripts/paper_b/generate_paper_b_assets.py:142`, `scripts/paper_b/generate_paper_b_assets.py:165`).
