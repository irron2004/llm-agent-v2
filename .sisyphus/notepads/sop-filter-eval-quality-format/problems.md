# Problems (append-only)

- Need a machine-checkable answer format so formatting consistency can be measured and enforced.
- Need richer evaluation artifacts (top_docs with metadata + full answer) to audit misses and faithfulness without reruns.
