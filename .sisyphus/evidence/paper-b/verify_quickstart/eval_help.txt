usage: run_paper_b_eval.py [-h] [--api-base-url API_BASE_URL]
                           [--queries QUERIES] [--out-dir OUT_DIR]
                           [--repeats REPEATS]
                           [--timeout-seconds TIMEOUT_SECONDS]
                           [--mode-name MODE_NAME]
                           [--deterministic DETERMINISTIC] [--limit LIMIT]

Run Paper B stability evaluation

options:
  -h, --help            show this help message and exit
  --api-base-url API_BASE_URL
  --queries QUERIES
  --out-dir OUT_DIR
  --repeats REPEATS
  --timeout-seconds TIMEOUT_SECONDS
  --mode-name MODE_NAME
                        Mode label written to each per-call row
  --deterministic DETERMINISTIC
                        Deterministic flag for this run (true/false)
  --limit LIMIT
