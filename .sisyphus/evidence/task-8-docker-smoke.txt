Task 8 evidence (2026-03-11)

Commands:
- make dev-up
- cd backend && uv run python -m scripts.evaluation.run_chat_flow_retrieval_rrf_eval --api-base-url http://localhost:8011 --queries scripts/evaluation/fixtures/rrf_smoke.jsonl --out /tmp/rrf_eval.jsonl
- docker compose logs api-dev --since 10m > /tmp/api-dev-rrf.log

Checks:
- grep pattern rank.rrf in /tmp/api-dev-rrf.log: no matches
- grep pattern "falling back to script_score" in /tmp/api-dev-rrf.log: no matches

Artifact:
- /tmp/rrf_eval.jsonl
