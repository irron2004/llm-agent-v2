# Production profile overrides
API_PORT=8001
FRONTEND_PORT=9097

# Frontend(prod) -> API(prod)
NGINX_API_PROXY_PASS=http://api:8000/api/

# Disable legacy compatibility routes in production
API_ENABLE_LEGACY_COMPAT_ROUTES=false
